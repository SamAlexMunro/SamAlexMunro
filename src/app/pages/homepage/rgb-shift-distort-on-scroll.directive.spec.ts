import { RgbShiftDistortOnScrollDirective } from './rgb-shift-distort-on-scroll.directive';

describe('RgbShiftDistortOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RgbShiftDistortOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
