import { TranslateToCanvasCoordsDirective } from './translate-to-canvas-coords.directive';

describe('TranslateToCanvasCoordsDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslateToCanvasCoordsDirective();
    expect(directive).toBeTruthy();
  });
});
